# PULSE · Hackathon submission copy

Replace placeholders only after the final deployment and live-payment checks pass.

## Main X post

Markets move continuously. Market intelligence should be just as callable.

Introducing **PULSE**—OKX spot intelligence for humans and AI agents.

✓ Live OKX tickers + candles
✓ Searchable live spot pairs + X Layer token discovery
✓ Structured Grok market scenarios
✓ Token + pre-trade safety
✓ Web, REST, MCP, and TypeScript SDK
✓ Pay per signal in USDT0 on X Layer via x402
✓ Built-in OKB → USDT0 funding with OKX Onchain OS

No subscription. No custodial account. One request, one payment, one usable answer.

Demo: <VIDEO_URL>
Product: <APP_URL>
Code: <GITHUB_URL>
ASP: <OKX_AI_LISTING_URL>

#OKXAI #OKXAIGenesis #XLayer #A2MCP #x402

## Optional technical reply

PULSE uses live OKX spot market context as the ground truth for paid Grok analysis. Traders choose only from returned OKX instruments, while the Safety workspace offers a source-labeled X Layer token catalog plus manual address entry. Free chart refreshes never erase the current report. The browser checks USDT0 before requesting an x402 signature, and one header wallet connection powers OKB/USDT0 balances plus a native OKX Exchange OS funding flow.

Agents get the same capability through MCP with structured JSON. Safety is explicitly scoped to X Layer chain 196: the free contract inspector reports live RPC evidence, while paid scores remain deterministic, explainable prototype heuristics and do not claim to be audits.

## Form field cheat sheet

| Field | Answer |
| --- | --- |
| Product | PULSE |
| Category | Trading / A2MCP |
| Tagline | OKX spot intelligence. Pay per signal on X Layer. |
| Network | X Layer (`eip155:196`) |
| Payment asset | USDT0 (`0x779d…3736`) |
| MCP | `<APP_URL>/mcp` |
| Metadata | `<APP_URL>/v1/metadata` |
| Demo | `<VIDEO_URL>` |
| Repository | `<GITHUB_URL>` |
| Listing | `<OKX_AI_LISTING_URL>` |

## Submission checklist

- [ ] All placeholders replaced
- [ ] Brand is PULSE in product, metadata, video, post, and listing
- [ ] Public URLs work in an incognito window
- [ ] Live versus mock payment mode is represented honestly
- [ ] Video contains no secrets or private wallet details
- [ ] Repository README matches the deployed release
- [ ] License and required hackathon terms reviewed
