# PULSE V5 implementation status

This is the acceptance gate for the 19-section product-change request. `Complete` means code, user-facing behavior, and proportionate automated verification exist. Configuration or documentation alone does not count as implementation.

| # | Requirement | Status | Implemented evidence | Remaining acceptance work |
|---|---|---|---|---|
| 1 | Existing compatibility | Complete | Legacy REST paths, GET discovery probes, MCP names/challenges/replay, SDK methods, schemas, and X Layer paid replay have regression tests. | Run the same suite as a production canary before each network rollout. |
| 2 | Shared application and payment architecture | Complete | Shared handlers sit behind immutable per-request network selection; `/xlayer`, `/base`, `/arbitrum`, `/arc` aliases select OKX, CDP, or Circle before expensive work. Signed OKX/CDP requests use an inline `verify → settle → handler` boundary, preventing provider or Grok work before successful settlement; official SDK middleware remains responsible for unpaid 402/Bazaar challenges. | Live low-value settlement certification belongs to section 18. |
| 3 | Polymarket identifiers and normalization | Complete | Condition ID is canonical; Gamma/event/question/slug/CLOB outcome IDs are retained; mapping and eligibility are revalidated; rejected selections are explicit and never substituted. | None for the agreed first release. |
| 4 | Prediction Markets UI workflow | Complete | URL-derived primary selection, trending/search, explicit additions, executable prices/spread/freshness, source warnings, recoverable reports, and user-selectable fused/prediction/divergence/event-risk products exist. Fused is the default flagship and collects the OKX instrument/timeframe explicitly. | Browser release matrix is tracked in section 18. |
| 5 | Polymarket data requirements | Complete | Public Gamma, CLOB order book/history, Data API open interest, bounded retry/timeout, short cache, observed times, stale/partial reporting, and no unnecessary private keys. A reproducible live readiness probe passed Gamma identifier reload, CLOB book/history, and Data open interest with stable outcome-token mapping; geographic `restricted` state remained explicitly ineligible. | WebSocket is not required for the agreed read/analysis release. |
| 6 | Parallel execution, resilience, latency | Complete | Independent sources run in parallel; optional sources settle independently; Polymarket has bounded retries/timeouts/cache; stale/partial policy and request latency metrics exist. | Production SLO certification remains section 18. |
| 7 | Grok architecture and structured output | Complete | Grok consumes prepared context, strict output keys are supplied to xAI and independently revalidated locally (including rejection of missing/extra fields and mixed-type arrays), conservative input bounds reject before the provider call, output-token limits are sent to xAI, and actual usage/cost fields are recorded using xAI's published Grok 4.3 rates reviewed 2026-08-03. Arc provider review keeps fixture-by-default/direct-xAI-live and identifies BlockRun only as a gated future adapter. | Recheck the current model rate immediately before live rollout. |
| 8 | Arc Testnet and AI cost | Complete | Arc payment routing, Circle batching, deterministic fixture reports, independent fixture/live control, hard input/output limits, pre-payment IP checks, atomic per-wallet hourly/daily controls, and worst-case daily cost reservations exist. Upstash makes production counters durable and shared. Live mode fails closed when model prices are unset. | Live mode remains a rollout gate, not a code gap. |
| 9 | Human web-wallet architecture | Code complete; operator certification pending | Reown AppKit + Wagmi + Viem supports the four approved networks with injected fallback; EIP-1193 switch/add/rejection behavior, provider identity, account/chain reconciliation, explicit disconnect, and fail-closed fresh-balance checks are automated. | Execute the physical OKX Wallet, MetaMask, Rabby, WalletConnect mobile, and Base Account release matrix during localhost acceptance. |
| 10 | Network-aware browser payment client | Complete | Per-network OKX/CDP/Circle construction exists; before signing, automated checks validate x402 version, scheme, selected network, native payment token, server-published configured price, configured payee, and canonical resource. Chain-add and rejection paths are tested. | Physical provider certification remains section 18. |
| 11 | Balances and funding UX | Complete | The shared funding drawer is available from both spot and prediction/fused workflows. It provides balances, X Layer OKB→USD₮0, Base/Arbitrum native ETH→native USDC, and Arc available/pending/finality/deposit flows. Real low-value Base, Arbitrum, and Arc transactions succeeded with the authorized test wallet. | Physical mobile-wallet certification is section 18. |
| 12 | Multichain UI, theme, and visual state | Complete | Every web surface uses selected-chain accents, identity, provider, asset, funding language, and Arc Testnet labelling with responsive layouts. | Physical-device visual certification is section 18. |
| 13 | Waiting for x402 deliverables | Complete | Canonical spot, prediction, fused, divergence, and event-risk services return immediate `202` with an opaque recovery token, persisted real stages, polling, elapsed state, durable same-browser recovery after refresh or closure, final private report, partial/failure/reconciliation states, visible receipt/settlement evidence, and explicit no-repayment guidance. Spot and prediction recovery namespaces cannot overwrite each other. Legacy X Layer spot aliases intentionally preserve their synchronous response contract. | Durable worker health is verified during rollout. |
| 14 | Payment idempotency and receipts | Complete | Full namespaced key, atomic acquire, authenticated reads, replay binding, private reports, and two receipt-bound automatic regenerations exist. Normalized receipts persist provider, CAIP-2 network and chain ID, asset, amount, actual decoded payer when available, payee, authorization, resource/request hashes, verification/settlement result, explicit synchronous-onchain/Gateway-batch/mock mode, finality scope (including unknown Arbitrum parent finality), timestamps, optional settlement transaction, final report linkage, and post-settlement failure/reconciliation state. | Live settlement certification is section 18. |
| 15 | Discovery and marketplace publication | Complete | ASP multichain metadata and official Bazaar v2 extension publish descriptions, POST examples, JSON schemas, provider/assets/payees; publication is flag-gated. The Circle listing pack supplies Arc Testnet/test-value disclosure, exact service paths, durable delivery, Gateway-batch receipt semantics, safety copy, and publication gates. Static HTML and `llms.txt` describe the unified multichain + Polymarket product. | Bazaar/Circle catalog appearance requires the corresponding successful deployed settlement and external publication step. |
| 16 | Safety modules and live evidence | Complete | All four networks expose live RPC contract/proxy and ERC-20 interface evidence plus exact non-broadcast transaction simulation. API RPC primaries/fallbacks are environment-controlled, tried in order, and chain-ID checked; production templates require a production-grade Arbitrum primary. Provider, block/time, methods, limitations, partial/unavailable states, and `unknown` verdict semantics are explicit. Legacy heuristic scores remain X Layer-only. | Add third-party ownership/liquidity/source-verification adapters only when they can return attributable evidence; absent fields must remain unknown. |
| 17 | Metrics, observability, and cost | Complete | Correlation-aware JSON events and Prometheus series cover HTTP latency/errors; OKX/Gamma/CLOB/Data/xAI/CDP provider latency, errors, retries, cache hits/age; payment challenge and synchronous verify-settle latency/failures/amount; job stages and unfinished count; completed/partial/failed/recovered reports; input/cached/reasoning/output tokens and estimated cost. Arc additionally enforces a durable daily ceiling. | Configure external scraping/dashboard/notification targets at deployment. SDKs expose OKX/CDP/Circle synchronous verify+settle as one middleware boundary, so it is labelled `verify_settle`, not falsely split into invented phase timings. |
| 18 | Testing and migration | Code complete; operator certification pending | The full automated workspace suite protects registry, normalization, selection, strict model-output validation, cost accounting, schemas, provider switching, RPC failover, balance guards, idempotency, receipts, private reports, durable recovery, and compatibility. Deterministic Arc E2Es and live provider canaries passed. Authorized low-value settlement-to-report tests passed on Base and X Layer, including X Layer USD₮0; Base/Arbitrum swaps and an Arc Gateway deposit also passed. | Complete the documented localhost physical-wallet/browser matrix, then—only after explicit release approval—certify each enabled network from its final deployed origin and verify public discovery. |
| 19 | Pull-request and rollout plan | Complete | Additive sequence, per-network gates, evidence, alerts, rollback behavior, and post-deploy commands are in `V5_PRODUCTION_RUNBOOK.md`. | Assign named human owners during release scheduling. |

## Current release gate

The implementation is ready for operator localhost end-to-end acceptance. Do not describe it as production-certified until the physical-wallet/browser matrix, final deployed-origin settlement checks, and external marketplace visibility checks pass. Those are release certifications, not missing application code.
